<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
    <%- typeof style !== 'undefined' ? style : '' %>
</head>

<body class="bg-slate-50 min-h-screen flex overflow-x-hidden">
    <%- include('../writer/components/sidebar') %>

    <div class="flex-1 flex flex-col min-w-0 lg:ml-64 transition-all duration-300">
        <%- include('../writer/components/header') %>

        <main class="p-4 md:p-8">
            <%- body %>
        </main>
    </div>

    <!-- Global Modal System -->
    <%- include('../partials/modal-system') %>

    <!-- Global Chat Modal -->
    <%- include('../partials/global-chat-modal', { userRole: 'writer', userId: typeof profile !== 'undefined' ? profile.user_id : null }) %>
    
    <!-- Enterprise Components -->
    <script src="/js/enterprise-components.js"></script>

    <%- script %>
</body>

</html>