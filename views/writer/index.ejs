<section id="page-overview" class="writer-page space-y-6">
    <div class="flex justify-between items-end">
        <div>
            <h2 class="text-2xl font-bold text-slate-800 tracking-tight">Writer Overview</h2>
            <p class="text-slate-500">Track your pending assignments and stats.</p>
        </div>
        <div class="text-right">
             <span class="text-xs font-bold text-slate-400 uppercase">Words Today</span>
             <p class="text-xl font-black text-indigo-600">2,450 / 5,000</p>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        <div class="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
            <div class="flex justify-between items-start mb-4">
                <div class="p-2 bg-indigo-50 text-indigo-600 rounded-lg"><i class="fi fi-rr-time-fast"></i></div>
                <span class="text-xs font-bold text-green-500 bg-green-50 px-2 py-1 rounded">+2 New</span>
            </div>
            <p class="text-slate-500 text-sm font-medium">New Enquiries</p>
            <p class="text-2xl font-bold text-slate-800">12</p>
        </div>
        </div>

    <script>
        // Overview Page logic placeholder (charts/data-fetching)
    </script>
</section>


<section id="page-new-tasks" class="writer-page hidden space-y-6">
    <h2 class="text-2xl font-bold text-slate-800">Assignment Enquiries</h2>
    
    <div class="bg-white rounded-2xl border border-slate-200 overflow-hidden">
        <table class="w-full text-left">
            <thead class="bg-slate-50 border-b border-slate-200">
                <tr>
                    <th class="px-6 py-4 text-xs font-bold text-slate-500 uppercase">Query Code</th>
                    <th class="px-6 py-4 text-xs font-bold text-slate-500 uppercase">Deadline</th>
                    <th class="px-6 py-4 text-xs font-bold text-slate-500 uppercase text-right">Action</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-100">
                <tr>
                    <td class="px-6 py-4 font-bold text-indigo-600">2025SS0901</td>
                    <td class="px-6 py-4 text-sm text-slate-600">Oct 12, 2025</td>
                    <td class="px-6 py-4 text-right">
                        <button onclick="handleAccept('2025SS0901')" class="bg-indigo-600 text-white px-4 py-2 rounded-lg text-xs font-bold hover:bg-indigo-700 transition-colors">Accept Task</button>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <script>
        async function handleAccept(code) {
            const confirmed = await showConfirmModal(
                'Accept Assignment',
                `Are you sure you want to accept assignment ${code}?`,
                'success'
            );
            if (confirmed) {
                showToast(`Accepting assignment: ${code}`, 'info');
                // Logic for accepting via API
            }
        }
    </script>
</section>