<!-- ADMIN SIDEBAR -->
<aside class="w-64 bg-white border-r border-slate-200 shadow-sm flex flex-col">
  
  <!-- LOGO -->
  <div class="p-6 border-b border-slate-100">
    <h1 class="text-2xl font-black text-slate-900">
      <span class="text-indigo-600">A</span>366 ADMIN
    </h1>
  </div>

  <!-- NAVIGATION -->
  <nav class="flex-1 p-4 space-y-2 overflow-y-auto">
    <a href="/admin" class="<%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
      <i class="fi fi-rr-chart-histogram"></i> Dashboard
    </a>

    <!-- QUERY MANAGEMENT -->
    <div class="pt-2">
      <p class="text-xs font-black text-slate-500 uppercase tracking-widest px-4 mb-2">OPERATIONS</p>
      
      <a href="/admin/queries" class="<%= typeof currentPage !== 'undefined' && currentPage === 'queries' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-inbox"></i> Queries
      </a>

      <a href="/admin/payments" class="<%= typeof currentPage !== 'undefined' && currentPage === 'payments' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-credit-card"></i> Payments
      </a>

      <a href="/admin/tasks" class="<%= typeof currentPage !== 'undefined' && currentPage === 'tasks' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-briefcase"></i> Tasks
      </a>

      <a href="/admin/assignments" class="<%= typeof currentPage !== 'undefined' && currentPage === 'assignments' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-users"></i> Assignments
      </a>

      <a href="/admin/qc/pending" class="<%= typeof currentPage !== 'undefined' && currentPage === 'qc' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-check-double"></i> Quality Control
      </a>

      <a href="/admin/delivery/ready" class="<%= typeof currentPage !== 'undefined' && currentPage === 'delivery' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-paper-plane"></i> Delivery
      </a>
    </div>

    <!-- MANAGEMENT -->
    <div class="pt-2">
      <p class="text-xs font-black text-slate-500 uppercase tracking-widest px-4 mb-2">MANAGEMENT</p>
      
      <a href="/admin/users" class="<%= typeof currentPage !== 'undefined' && currentPage === 'users' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-users"></i> Users
      </a>

      <a href="/admin/audit/logs" class="<%= typeof currentPage !== 'undefined' && currentPage === 'audit' ? 'bg-indigo-600 text-white' : 'text-slate-600 hover:bg-slate-100' %> px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-file-audit"></i> Audit Logs
      </a>

      <a href="#" class="text-slate-600 hover:bg-slate-100 px-4 py-3 rounded-lg font-semibold text-sm transition-all flex items-center gap-2">
        <i class="fi fi-rr-settings"></i> Settings
      </a>
    </div>
  </nav>

  <!-- FOOTER -->
  <div class="p-4 border-t border-slate-100 space-y-2">
    <a href="/auth/logout" class="text-slate-600 hover:bg-red-50 hover:text-red-600 px-4 py-2 rounded-lg font-semibold text-sm transition-all flex items-center gap-2 w-full">
      <i class="fi fi-rr-sign-out-alt"></i> Logout
    </a>
  </div>

</aside>
